version: '3.8'

services:
  cybersillbot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cybersillbot
    env_file:
      - .env
    command: ["python", "bot.py"]
    ports:
      - "8002:8000"
    restart: unless-stopped 